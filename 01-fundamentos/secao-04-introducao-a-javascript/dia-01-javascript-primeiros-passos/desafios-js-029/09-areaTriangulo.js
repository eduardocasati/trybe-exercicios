/* Crie um algoritmo que calcule a área de um triângulo.
Área = (base * altura) / 2 e exiba no formato:
A área do triângulo de base X e altura Y é: Z */

let base = 30;
let altura = 35;

let area = (base * altura) / 2;

console.log('A área do triângulo de base', base, 'e altura', altura, 'é:', area);